/**
 * name: scared
 * aliases: []
 * description: Reacción anime: scared (asustado)
 * category: Reacciones
 */
import { sendReaction } from './_common.js'
export async function run(ctx){ await sendReaction(ctx,'scared') }
